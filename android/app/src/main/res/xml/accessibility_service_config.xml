<?xml version="1.0" encoding="utf-8"?>
<!--
  The narrowest configuration that can still tell which app is in front.

  `typeWindowStateChanged` alone is enough to know that the foreground app changed; Curfew asks for
  no content events, no key filtering and no screen retrieval, so it cannot read what is on screen
  even if a future version wanted to. `canRetrieveWindowContent` is deliberately false.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
