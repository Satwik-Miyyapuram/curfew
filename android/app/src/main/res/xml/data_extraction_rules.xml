<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing is backed up or transferred to a new device.

  A backup of Curfew's own database would be a way to roll a running lock back to an earlier state,
  and a cloud copy of it would contradict the promise that nothing leaves the device. Setup on a new
  device is a deliberate act, done through the app's own export.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
